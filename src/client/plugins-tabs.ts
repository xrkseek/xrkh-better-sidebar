/**
 * Recommended TAB plugins catalog (Side card → add tab plugin).
 * Kept empty for the XRKH-native package; community entries live elsewhere.
 */
import type { PluginEntry } from './plugins-shared.ts'

export const builtinTabPlugins: readonly PluginEntry[] = []
